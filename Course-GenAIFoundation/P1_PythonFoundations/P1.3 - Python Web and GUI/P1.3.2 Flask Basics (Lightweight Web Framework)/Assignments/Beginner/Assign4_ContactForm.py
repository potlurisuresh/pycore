"""
Beginner Assignment 4: Contact Form (with Static HTML Template)

Scenario:
Create a contact form that collects a user's name and message. After submission, display a thank you message with the user's name. Use a static HTML template (no Jinja) for the form page.

Objective:
- Build a Flask app with a `/contact` route
- Display a form for name and message (GET) using a static HTML template
- Show a thank you message with the name (POST)

Tasks:
1. Create a Flask app
2. Create a `templates` folder and a `contact.html` file (no Jinja)
3. Define the `/contact` route (GET and POST)
4. On GET, render the static template; on POST, show thank you message

Hints:
- Use render_template('contact.html') for GET
- Use request.form to get form data

Rubric:
- Form displays and submits: 40%
- Thank you message works: 40%
- Code clarity and comments: 20%
"""

# Your code here
